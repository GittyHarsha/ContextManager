{
	"name": "context-manager",
	"publisher": "local-dev",
	"displayName": "ContextManager - AI Project Memory",
	"version": "2.12.0",
	"description": "Persistent project memory for Copilot: knowledge cards, multi-project session routing, BM25 search, queue-aware project-scoped chat tools, AI/template workflows, dedicated model routing, auto-capture, and a full dashboard.",
	"icon": "ContextManager.png",
	"galleryBanner": {
		"color": "#1e1e1e",
		"theme": "dark"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/GittyHarsha/ContextManager.git"
	},
	"homepage": "https://gittyharsha.github.io/ContextManager",
	"bugs": {
		"url": "https://github.com/GittyHarsha/ContextManager/issues"
	},
	"author": {
		"name": "Harsha Narayana P"
	},
	"engines": {
		"vscode": "^1.100.0"
	},
	"categories": [
		"AI",
		"Chat",
		"Programming Languages",
		"Other"
	],
	"keywords": [
		"ai",
		"copilot",
		"code explanation",
		"knowledge management",
		"todo",
		"project management",
		"context",
		"chat participant",
		"codebase navigation",
		"architecture",
		"code understanding",
		"documentation"
	],
	"activationEvents": [
		"*"
	],
	"preview": false,
	"main": "./dist/extension.js",
	"contributes": {
		"viewsContainers": {
			"activitybar": [
				{
					"id": "contextManager",
					"title": "ContextManager",
					"icon": "$(book)"
				}
			]
		},
		"views": {
			"contextManager": [
				{
					"id": "contextManagerProjects",
					"name": "Projects",
					"icon": "$(folder)",
					"contextualTitle": "ContextManager Projects"
				}
			]
		},
		"viewsWelcome": [
			{
				"view": "contextManagerProjects",
				"contents": "No projects yet.\n[Create Project](command:contextManager.createProject)"
			}
		],
		"chatSessions": [
			{
				"type": "ctx-sessions",
				"name": "ctx",
				"displayName": "ContextManager Sessions",
				"icon": "$(book)",
				"welcomeTitle": "ContextManager Session History",
				"welcomeMessage": "Browse your TODO agent runs and branch sessions",
				"description": "View TODO agent runs and branch session history from ContextManager projects",
				"order": 10
			}
		],
		"commands": [
			{
				"command": "contextManager.explainSymbol",
				"title": "Explain",
				"category": "ContextManager"
			},
			{
				"command": "contextManager.explainUsage",
				"title": "Explain Usage",
				"category": "ContextManager"
			},
			{
				"command": "contextManager.explainRelationships",
				"title": "Explain Relationships",
				"category": "ContextManager"
			},
			{
				"command": "contextManager.clearCache",
				"title": "Clear Explanation Cache",
				"category": "ContextManager"
			},
			{
				"command": "contextManager.openDashboard",
				"title": "Open Dashboard",
				"category": "ContextManager",
				"icon": "$(dashboard)"
			},
			{
				"command": "contextManager.createProject",
				"title": "Create Project",
				"category": "ContextManager",
				"icon": "$(add)"
			},
			{
				"command": "contextManager.deleteProject",
				"title": "Delete Project",
				"category": "ContextManager",
				"icon": "$(trash)"
			},
			{
				"command": "contextManager.setActiveProject",
				"title": "Set Active Project",
				"category": "ContextManager",
				"icon": "$(check)"
			},
			{
				"command": "contextManager.deselectProject",
				"title": "Deselect Project",
				"category": "ContextManager",
				"icon": "$(close)"
			},
			{
				"command": "contextManager.refreshProjects",
				"title": "Refresh Projects",
				"category": "ContextManager",
				"icon": "$(refresh)"
			},
			{
				"command": "contextManager.smartSelect",
				"title": "Smart Select Knowledge Cards",
				"category": "ContextManager",
				"icon": "$(sparkle)"
			},
			{
				"command": "contextManager.embedProject",
				"title": "Embed Knowledge Cards",
				"category": "ContextManager",
				"icon": "$(database)"
			},
			{
				"command": "contextManager.clearEmbeddings",
				"title": "Clear Embeddings",
				"category": "ContextManager",
				"icon": "$(trash)"
			},
			{
				"command": "contextManager.captureExchange",
				"title": "Capture Chat Exchange",
				"category": "ContextManager",
				"icon": "$(record)"
			},
			{
				"command": "contextManager.saveAsCard",
				"title": "Save Last Chat as Knowledge Card",
				"category": "ContextManager",
				"icon": "$(save-as)"
			},
			{
				"command": "contextManager.installHooks",
				"title": "Install Hooks",
				"category": "ContextManager",
				"icon": "$(plug)"
			},
			{
				"command": "contextManager.installCopilotCliPluginHooks",
				"title": "Install Copilot CLI Plugin Hooks",
				"category": "ContextManager",
				"icon": "$(cloud-download)"
			},
			{
				"command": "contextManager.openPluginScaffold",
				"title": "Open Plugin Scaffold",
				"category": "ContextManager",
				"icon": "$(package)"
			},
			{
				"command": "contextManager.installClaudeCodeHooks",
				"title": "Install Claude Code Hooks",
				"category": "ContextManager",
				"icon": "$(plug)"
			}
		],
		"menus": {
			"commandPalette": [
				{
					"command": "contextManager.deleteProject",
					"when": "false"
				},
				{
					"command": "contextManager.setActiveProject",
					"when": "false"
				},
				{
					"command": "contextManager.deselectProject",
					"when": "false"
				},
				{
					"command": "contextManager.refreshProjects",
					"when": "false"
				}
			],
			"editor/context": [
				{
					"command": "contextManager.explainSymbol",
					"when": "editorTextFocus",
					"group": "1_contextManager@1"
				},
				{
					"command": "contextManager.explainUsage",
					"when": "editorTextFocus",
					"group": "1_contextManager@2"
				},
				{
					"command": "contextManager.explainRelationships",
					"when": "editorTextFocus",
					"group": "1_contextManager@3"
				}
			],
			"view/title": [
				{
					"command": "contextManager.createProject",
					"when": "view == contextManagerProjects",
					"group": "navigation@1"
				},
				{
					"command": "contextManager.refreshProjects",
					"when": "view == contextManagerProjects",
					"group": "navigation@2"
				},
				{
					"command": "contextManager.openDashboard",
					"when": "view == contextManagerProjects",
					"group": "navigation@3"
				},
				{
					"command": "contextManager.deselectProject",
					"when": "view == contextManagerProjects",
					"group": "1_actions"
				}
			],
			"view/item/context": [
				{
					"command": "contextManager.setActiveProject",
					"when": "view == contextManagerProjects && viewItem == project",
					"group": "inline@1"
				},
				{
					"command": "contextManager.deselectProject",
					"when": "view == contextManagerProjects && viewItem == project-active",
					"group": "inline@1"
				},
				{
					"command": "contextManager.deleteProject",
					"when": "view == contextManagerProjects && viewItem =~ /^project/",
					"group": "context@1"
				}
			]
		},
		"configuration": {
			"title": "ContextManager",
			"properties": {
				"contextManager.showStatusBar": {
					"type": "boolean",
					"default": true,
					"description": "Show active project information in the status bar"
				},
				"contextManager.confirmDelete": {
					"type": "boolean",
					"default": true,
					"description": "Show confirmation dialog before deleting projects, TODOs, or knowledge cards"
				},
				"contextManager.maxKnowledgeCardsInContext": {
					"type": "number",
					"default": 10,
					"minimum": 1,
					"maximum": 20,
					"description": "Maximum number of knowledge cards injected into AI prompts and returned by Smart Select"
				},
				"contextManager.explanation.expandContext": {
					"type": "boolean",
					"default": true,
					"description": "Automatically expand code context when explaining symbols"
				},
				"contextManager.prompts.globalInstructions": {
					"type": "string",
					"default": "",
					"editPresentation": "multilineText",
					"markdownDescription": "Custom instructions appended to **every** prompt (all commands). Use this for global style or behavior preferences."
				},
				"contextManager.prompts.distillObservations": {
					"type": "string",
					"default": "",
					"editPresentation": "multilineText",
					"markdownDescription": "Custom prompt for **Distill Observations** (extracts conventions, tool hints, and working notes from session observations). Leave empty to use built-in prompt."
				},
				"contextManager.prompts.distillQueue": {
					"type": "string",
					"default": "",
					"editPresentation": "multilineText",
					"markdownDescription": "Custom prompt for **Distill Queue** (synthesizes card queue entries into knowledge card proposals). Leave empty to use built-in prompt."
				},
				"contextManager.prompts.synthesizeCard": {
					"type": "string",
					"default": "",
					"editPresentation": "multilineText",
					"markdownDescription": "Custom prompt for **AI Draft / Synthesize Card** (generates a single knowledge card from selected sources in the card editor). Leave empty to use built-in prompt."
				},
				"contextManager.intelligence.enableTieredInjection": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable automatic injection of conventions and top tool hints into every chat prompt (Tier 1+2). Disable to rely on tool queries only."
				},
				"contextManager.intelligence.tier1MaxTokens": {
					"type": "integer",
					"default": 400,
					"minimum": 100,
					"maximum": 1000,
					"markdownDescription": "Token budget for Tier 1 (always-injected) learnings: confirmed conventions + top tool hints."
				},
				"contextManager.intelligence.tier2MaxTokens": {
					"type": "integer",
					"default": 400,
					"minimum": 100,
					"maximum": 1000,
					"markdownDescription": "Token budget for Tier 2 (task-relevant) learnings: working notes and observed conventions matched by prompt keywords."
				},
				"contextManager.intelligence.injectConventions": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Include **conventions** in auto-injected intelligence context. Disable to stop conventions from being injected into non-@ctx prompts."
				},
				"contextManager.intelligence.injectWorkingNotes": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Include **working notes** in auto-injected intelligence context. Disable to stop working notes from being injected into non-@ctx prompts."
				},
				"contextManager.intelligence.injectToolHints": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Include **tool hints** in auto-injected intelligence context. Disable to stop tool hints from being injected into non-@ctx prompts."
				},
				"contextManager.intelligence.injectKnowledgeCards": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Include **knowledge cards** in auto-injected intelligence context. Disable to stop knowledge cards from being injected into non-@ctx prompts."
				},
				"contextManager.intelligence.enableStalenessTracking": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable file-based staleness tracking on working notes and knowledge cards. Items are flagged when their referenced files are modified."
				},
				"contextManager.intelligence.stalenessAgeDays": {
					"type": "integer",
					"default": 30,
					"minimum": 7,
					"maximum": 365,
					"markdownDescription": "Knowledge cards not updated within this many days are flagged as age-stale in the dashboard."
				},
				"contextManager.intelligence.autoLearn": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable the **auto-learning pipeline**. After every chat interaction, automatically extract tool hints (search fail→success patterns), working notes (file co-access relationships), and conventions (pattern statements in responses). All items are active immediately — disable individual ones in the dashboard."
				},
				"contextManager.intelligence.autoLearn.useLLM": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Use a lightweight **LLM call** (~200 input tokens) for convention and working-note extraction instead of regex-only matching. Dramatically improves precision. Falls back to regex if no model is available."
				},
				"contextManager.intelligence.autoLearn.discardThreshold": {
					"type": "integer",
					"default": 5,
					"minimum": 0,
					"maximum": 50,
					"markdownDescription": "Suppress a signal category after the user discards this many inferred items of that type. For example, after discarding 5 `naming` conventions, no more `naming` conventions will be auto-learned. Set to **0** to disable suppression."
				},
				"contextManager.intelligence.autoLearn.showInChat": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Show auto-learn results as a **notification toast** after chat interactions. When disabled, auto-learned items are only visible in the dashboard."
				},
				"contextManager.intelligence.autoLearn.modelFamily": {
					"type": "string",
					"default": "",
					"markdownDescription": "Preferred language model family for auto-learn LLM extraction. This runs in the background after each chat interaction. Leave empty to use the default available model. Use the dashboard for a dropdown of available models."
				},
				"contextManager.workflows.modelFamily": {
					"type": "string",
					"default": "",
					"markdownDescription": "Preferred language model family for **AI workflow actions**. Applies to create/update/append workflow actions that call the model. Template-only workflow actions ignore this setting. Leave empty to use the default available model."
				},
				"contextManager.knowledgeCards.synthesisModelFamily": {
					"type": "string",
					"default": "",
					"markdownDescription": "Preferred language model family for **AI Draft / Synthesize Card** in the dashboard editor. Leave empty to use the default available model."
				},
				"contextManager.intelligence.autoLearn.maxWorkingNotes": {
					"type": "integer",
					"default": 30,
					"minimum": 5,
					"maximum": 100,
					"markdownDescription": "Maximum number of **inferred** working notes per project. When the cap is reached, the oldest inferred note is evicted before adding a new one. Notes promoted to `observed` or `confirmed` are never evicted."
				},
				"contextManager.intelligence.autoLearn.maxToolHints": {
					"type": "integer",
					"default": 20,
					"minimum": 5,
					"maximum": 100,
					"markdownDescription": "Maximum number of tool hints per project. When the cap is reached, the least-used hint is evicted."
				},
				"contextManager.intelligence.autoLearn.maxConventions": {
					"type": "integer",
					"default": 15,
					"minimum": 5,
					"maximum": 50,
					"markdownDescription": "Maximum number of **inferred** conventions per project. When the cap is reached, the oldest inferred convention is evicted. Conventions promoted to `observed` or `confirmed` are never evicted."
				},
				"contextManager.intelligence.autoLearn.extractToolHints": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Extract **tool hints** from search fail→success patterns. When a search returns no results but a subsequent search with a different query succeeds, the working query is recorded as a hint."
				},
				"contextManager.intelligence.autoLearn.extractWorkingNotes": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Extract **working notes** from file co-access patterns. When multiple files across different directories are read together, the relationship is recorded as an insight."
				},
				"contextManager.intelligence.autoLearn.extractConventions": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Extract **conventions** from response text. When the AI response contains statements like _\"always use…\"_, _\"never call…\"_, or mentions naming conventions, the pattern is recorded."
				},
				"contextManager.intelligence.autoLearn.hintsPerRun": {
					"type": "integer",
					"default": 3,
					"minimum": 0,
					"maximum": 10,
					"markdownDescription": "Maximum tool hints to extract from a **single** chat interaction. Set to 0 to disable tool hint extraction per-run."
				},
				"contextManager.intelligence.autoLearn.notesPerRun": {
					"type": "integer",
					"default": 2,
					"minimum": 0,
					"maximum": 10,
					"markdownDescription": "Maximum working notes to extract from a **single** chat interaction. Set to 0 to disable note extraction per-run."
				},
				"contextManager.intelligence.autoLearn.conventionsPerRun": {
					"type": "integer",
					"default": 1,
					"minimum": 0,
					"maximum": 5,
					"markdownDescription": "Maximum conventions to extract from a **single** chat interaction. Set to 0 to disable convention extraction per-run."
				},
				"contextManager.intelligence.autoLearn.expiryDays": {
					"type": "integer",
					"default": 0,
					"minimum": 0,
					"maximum": 365,
					"markdownDescription": "Auto-expire **inferred** learnings after this many days if never promoted to `observed` or `confirmed`. Set to **0** to disable auto-expiry (items only evicted when cap is reached)."
				},
				"contextManager.autoCapture.enabled": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable **auto-capture** of observations from all chat participants (Copilot, @workspace, etc.). Every model response is recorded as a lightweight observation for session continuity. Unlike auto-learn, this captures from **all** participants, not just `@ctx`."
				},
				"contextManager.autoCapture.learnFromAllParticipants": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Run lightweight **LLM extraction** on non-@ctx chat interactions to learn conventions and working notes. This extends the auto-learn pipeline to cover normal Copilot Chat and other participants."
				},
				"contextManager.autoCapture.maxObservations": {
					"type": "integer",
					"default": 50,
					"minimum": 10,
					"maximum": 200,
					"markdownDescription": "Maximum number of observations to store in the circular buffer. Older observations are evicted when the buffer is full."
				},
				"contextManager.sessionTracking.enabled": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Track chat sessions in the Sessions tab. When disabled, no sessions are recorded and the Sessions tab stays empty. Hook capture and auto-capture continue to work independently."
				},
				"contextManager.hooks.sessionStart": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable the UserPromptSubmit hook path that injects selected cards and custom prompt instructions into each prompt."
				},
				"contextManager.hooks.postToolUse": {
					"type": "boolean",
					"default": false,
					"markdownDescription": "Enable hook-based capture of tool calls during agent sessions. **Warning:** creates an observation for every single tool call — very noisy."
				},
				"contextManager.hooks.preCompact": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Capture session state before VS Code compacts conversation history."
				},
				"contextManager.hooks.stop": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Capture the final session transcript when an agent session ends."
				},
				"contextManager.orchestrator.enabled": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable agent orchestration (registry + psmux send). Allows multiple CLI/VSCode/Claude sessions to coordinate."
				},
				"contextManager.tools.backgroundMode": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "When **enabled** (default), the `#saveCard` tool runs silently with only a brief status line — the chat session is never interrupted. When **disabled**, each tool invocation shows a confirmation dialog before proceeding."
				},
				"contextManager.search.enableFTS": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable BM25 full-text search (SQLite FTS4) for knowledge cards, cache, observations, and other entities. Provides ranked relevance search instead of simple keyword matching. Disable to fall back to basic keyword search."
				},
				"contextManager.search.maxCardResults": {
					"type": "integer",
					"default": 5,
					"minimum": 1,
					"maximum": 20,
					"markdownDescription": "Maximum number of card results returned by `#ctx` card searches. Higher values provide more context but consume more tokens."
				},
				"contextManager.search.maxSearchResults": {
					"type": "integer",
					"default": 10,
					"minimum": 1,
					"maximum": 50,
					"markdownDescription": "Maximum number of results returned by `#ctx` cross-entity search. Higher values provide broader context but consume more tokens."
				},
				"contextManager.search.snippetTokens": {
					"type": "integer",
					"default": 16,
					"minimum": 8,
					"maximum": 64,
					"markdownDescription": "Number of context tokens around each match highlight in search snippets. Higher values show more surrounding text per result."
				},
				"contextManager.internal.autoLearn.llmCostWarningShown": {
					"type": "boolean",
					"default": false,
					"markdownDescription": "Internal flag — tracks whether the one-time auto-learn LLM cost notification has been shown. Do not modify manually.",
					"scope": "application"
				},
				"contextManager.autoDistill.enabled": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Automatically distill observations into conventions and knowledge cards at compaction checkpoints."
				},
				"contextManager.autoDistill.intervalMinutes": {
					"type": "number",
					"default": 30,
					"minimum": 5,
					"markdownDescription": "Minimum minutes between automatic distillation runs per project."
				},
				"contextManager.autoDistill.dedupThreshold": {
					"type": "number",
					"default": 0.8,
					"minimum": 0.5,
					"maximum": 1,
					"markdownDescription": "Jaccard similarity threshold for detecting duplicate conventions/notes. 0.5=loose (more merges), 1.0=strict (exact matches only). Default: 0.8"
				},
				"contextManager.saveAsCard.smartMerge": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Enable smart merge detection when saving knowledge cards (checks for semantic overlap with existing cards)."
				},
				"contextManager.cardQueue.enabled": {
					"type": "boolean",
					"default": true,
					"markdownDescription": "Automatically detect card-worthy content in chat responses and add to review queue for approval."
				},
				"contextManager.cardQueue.minResponseLength": {
					"type": "number",
					"default": 300,
					"minimum": 50,
					"maximum": 5000,
					"markdownDescription": "Minimum character length of a model response to queue it as a card candidate. Responses shorter than this are skipped. Default: 300"
				},
				"contextManager.cardQueue.maxSize": {
					"type": "number",
					"default": 30,
					"minimum": 5,
					"maximum": 100,
					"markdownDescription": "Maximum number of candidates in the review queue. Oldest items are dropped when the limit is reached."
				},
				"contextManager.cardQueue.distillBatchSize": {
					"type": "number",
					"default": 2,
					"minimum": 1,
					"maximum": 10,
					"markdownDescription": "Number of candidates processed per LLM call during distillation. Lower = more calls but no content truncation. Higher = fewer calls but may hit context limits."
				},
				"contextManager.cardQueue.maxCardsPerDistill": {
					"type": "number",
					"default": 12,
					"minimum": 1,
					"maximum": 30,
					"markdownDescription": "Maximum number of knowledge cards to extract in a single distill run."
				},
				"contextManager.context.stubLines": {
					"type": "number",
					"default": 5,
					"minimum": 1,
					"maximum": 30,
					"markdownDescription": "Lines of code captured per anchor stub when a knowledge card is extracted from chat sessions."
				}
			}
		},
		"languageModelTools": [
			{
				"name": "contextManager_ctx",
				"tags": [
					"search",
					"find",
					"query",
					"conventions",
					"notes",
					"hints",
					"knowledge",
					"intelligence",
					"memory",
					"learn"
				],
				"toolReferenceName": "ctx",
				"displayName": "Project Memory",
				"modelDescription": "Unified tool for ALL project knowledge operations. When multiple ContextManager projects exist, pass `project` explicitly using the exact project ID, exact project name, or exact workspace root path. Modes:\n\n**search** (default): BM25 search across all entity types. Use entityTypes to filter: 'convention', 'workingNote', 'toolHint', 'card', 'cache', 'observation', 'session', 'agentMessage', 'project'. Examples: query='auth' entityTypes=['convention','workingNote'] to search only conventions and notes.\n\n**list**: List all items of a type. Set type to 'conventions', 'workingNotes', 'toolHints', 'cards', or 'queue'. No query needed.\n\n**learn**: Create new intelligence. Set learnType to 'convention' (needs category+title+content), 'toolHint' (needs toolName+pattern+example), or 'workingNote' (needs subject+insight).\n\n**getCard**: Read a knowledge card by ID.\n\n**getQueueItem**: Read a queued card candidate by ID.\n\n**approveQueueItem**: Approve a queued card candidate into a knowledge card. Optional overrides: cardTitle, cardContent, cardCategory, cardTags.\n\n**rejectQueueItem**: Reject a queued card candidate.\n\n**distillQueue**: Synthesize queued card candidates into proposed knowledge cards. Optional candidateIds limits the set to distill.\n\n**clearQueue**: Remove all queued card candidates from the selected project.\n\n**retrospect**: End-of-task learning. Provide newConventions, newToolHints, knowledgeCards arrays.\n\n**fetch**: Fetch full observation details by IDs.",
				"canBeReferencedInPrompt": true,
				"icon": "$(search-fuzzy)",
				"inputSchema": {
					"type": "object",
					"properties": {
						"project": {
							"type": "string",
							"description": "Exact project ID, exact project name, or exact workspace root path. Required when multiple ContextManager projects exist."
						},
						"mode": {
							"type": "string",
							"enum": [
								"search",
								"list",
								"learn",
								"getCard",
								"getQueueItem",
								"approveQueueItem",
								"rejectQueueItem",
								"distillQueue",
								"clearQueue",
								"fetch",
								"retrospect"
							],
							"description": "Operation mode. Default 'search'.",
							"default": "search"
						},
						"query": {
							"type": "string",
							"description": "Search query (for mode='search'). Supports quoted phrases."
						},
						"entityTypes": {
							"type": "array",
							"items": {
								"type": "string",
								"enum": [
									"card",
									"convention",
									"workingNote",
									"toolHint",
									"cache",
									"observation",
									"session",
									"agentMessage",
									"project"
								]
							},
							"description": "Filter search to specific entity types."
						},
						"limit": {
							"type": "number",
							"description": "Max results for search. Default 10.",
							"default": 10
						},
						"type": {
							"type": "string",
							"enum": [
								"conventions",
								"workingNotes",
								"toolHints",
								"cards",
								"queue"
							],
							"description": "For mode='list': which type to list."
						},
						"learnType": {
							"type": "string",
							"enum": [
								"convention",
								"toolHint",
								"workingNote"
							],
							"description": "For mode='learn': what to create."
						},
						"id": {
							"type": "string",
							"description": "Card ID for mode='getCard' or queued candidate ID for queue item modes."
						},
						"candidateIds": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Optional queued candidate IDs to distill. Used by mode='distillQueue'."
						},
						"cardTitle": {
							"type": "string",
							"description": "Optional replacement title when approving a queued card candidate."
						},
						"cardContent": {
							"type": "string",
							"description": "Optional replacement content when approving a queued card candidate."
						},
						"cardCategory": {
							"type": "string",
							"enum": [
								"architecture",
								"pattern",
								"convention",
								"explanation",
								"note",
								"other"
							],
							"description": "Optional replacement category when approving a queued card candidate."
						},
						"cardTags": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Optional replacement tags when approving a queued card candidate."
						},
						"category": {
							"type": "string",
							"enum": [
								"architecture",
								"naming",
								"patterns",
								"testing",
								"tooling",
								"pitfalls"
							],
							"description": "Convention category (for learn convention)."
						},
						"title": {
							"type": "string",
							"description": "Title (for learn convention)."
						},
						"content": {
							"type": "string",
							"description": "Content (for learn convention)."
						},
						"confidence": {
							"type": "string",
							"enum": [
								"observed",
								"inferred"
							],
							"description": "Confidence level."
						},
						"learnedFrom": {
							"type": "string",
							"description": "Source of the convention."
						},
						"toolName": {
							"type": "string",
							"description": "Tool name (for learn toolHint)."
						},
						"pattern": {
							"type": "string",
							"description": "Search pattern (for learn toolHint)."
						},
						"antiPattern": {
							"type": "string",
							"description": "Anti-pattern (for learn toolHint)."
						},
						"example": {
							"type": "string",
							"description": "Example (for learn toolHint)."
						},
						"subject": {
							"type": "string",
							"description": "Subject (for learn workingNote)."
						},
						"insight": {
							"type": "string",
							"description": "Insight (for learn workingNote)."
						},
						"relatedFiles": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Related files (for learn workingNote)."
						},
						"relatedSymbols": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Related symbols (for learn workingNote)."
						},
						"observationIds": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Observation IDs for fetch mode."
						},
						"taskSummary": {
							"type": "string",
							"description": "Task summary (for retrospect)."
						},
						"newConventions": {
							"type": "array",
							"items": {
								"type": "object",
								"properties": {
									"category": {
										"type": "string"
									},
									"title": {
										"type": "string"
									},
									"content": {
										"type": "string"
									}
								},
								"required": [
									"title",
									"content"
								]
							},
							"description": "New conventions (for retrospect)."
						},
						"newToolHints": {
							"type": "array",
							"items": {
								"type": "object",
								"properties": {
									"toolName": {
										"type": "string"
									},
									"pattern": {
										"type": "string"
									},
									"antiPattern": {
										"type": "string"
									},
									"example": {
										"type": "string"
									}
								},
								"required": [
									"toolName",
									"pattern"
								]
							},
							"description": "New tool hints (for retrospect)."
						},
						"knowledgeCards": {
							"type": "array",
							"items": {
								"type": "object",
								"properties": {
									"title": {
										"type": "string"
									},
									"content": {
										"type": "string"
									},
									"category": {
										"type": "string"
									}
								},
								"required": [
									"title",
									"content"
								]
							},
							"description": "New knowledge cards (for retrospect)."
						}
					}
				}
			},
			{
				"name": "contextManager_saveKnowledgeCard",
				"tags": [
					"knowledge",
					"card",
					"save",
					"background",
					"memory",
					"folder"
				],
				"toolReferenceName": "saveCard",
				"displayName": "Save Knowledge Card",
				"modelDescription": "Silently saves a knowledge card to the selected project without interrupting the chat session. When multiple ContextManager projects exist, pass `project` explicitly using the exact project ID, exact project name, or exact workspace root path. The same tool can also list folders or create a folder before saving. For saving, provide title, content (markdown), optional category ('architecture'|'pattern'|'convention'|'explanation'|'note'|'other'), optional tags, optional source, and optional folder controls.",
				"canBeReferencedInPrompt": true,
				"icon": "$(notebook-edit)",
				"inputSchema": {
					"type": "object",
					"properties": {
						"project": {
							"type": "string",
							"description": "Exact project ID, exact project name, or exact workspace root path. Required when multiple ContextManager projects exist."
						},
						"action": {
							"type": "string",
							"description": "Action to perform. Use 'save' to save a card, 'listFolders' to inspect available folders, or 'createFolder' to create a folder without saving a card.",
							"enum": [
								"save",
								"listFolders",
								"createFolder"
							],
							"default": "save"
						},
						"title": {
							"type": "string",
							"description": "Title of the knowledge card. Required when action='save'."
						},
						"content": {
							"type": "string",
							"description": "Full markdown content of the card. Required when action='save'."
						},
						"category": {
							"type": "string",
							"description": "Category for the card. Default: 'note'.",
							"enum": [
								"architecture",
								"pattern",
								"convention",
								"explanation",
								"note",
								"other"
							]
						},
						"tags": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Optional list of tags for discovery."
						},
						"source": {
							"type": "string",
							"description": "Optional source reference (e.g. doc URL or file path)."
						},
						"folderMode": {
							"type": "string",
							"description": "For action='save': auto-pick a folder, force root, or use/create a named folder.",
							"enum": [
								"auto",
								"root",
								"named-folder"
							],
							"default": "auto"
						},
						"folderName": {
							"type": "string",
							"description": "Folder name to place the card into or create. Use with action='createFolder' or folderMode='named-folder'."
						},
						"parentFolderName": {
							"type": "string",
							"description": "Optional parent folder name when creating or resolving a nested folder."
						},
						"createFolderIfMissing": {
							"type": "boolean",
							"description": "When saving to a named folder, create the folder if it does not already exist.",
							"default": true
						}
					}
				}
			},
			{
				"name": "contextManager_getCard",
				"tags": [
					"knowledge",
					"card",
					"read",
					"get",
					"detail"
				],
				"toolReferenceName": "getCard",
				"displayName": "Get Knowledge Card",
				"modelDescription": "Reads the full content of a knowledge card by its ID. When multiple ContextManager projects exist, pass `project` explicitly using the exact project ID, exact project name, or exact workspace root path. Returns title, content, category, tags, anchors (code stubs that ground the card), and staleness warnings if anchor files have changed. Use this tool after finding a card via #ctx. Always prefer this over re-searching when you already have a card ID.",
				"canBeReferencedInPrompt": true,
				"icon": "$(open-preview)",
				"inputSchema": {
					"type": "object",
					"required": [
						"id"
					],
					"properties": {
						"project": {
							"type": "string",
							"description": "Exact project ID, exact project name, or exact workspace root path. Required when multiple ContextManager projects exist."
						},
						"id": {
							"type": "string",
							"description": "The knowledge card ID (from the project context index)."
						}
					}
				}
			},
			{
				"name": "contextManager_editKnowledgeCard",
				"tags": [
					"knowledge",
					"card",
					"edit",
					"update",
					"memory"
				],
				"toolReferenceName": "editCard",
				"displayName": "Edit Knowledge Card",
				"modelDescription": "Updates fields on an existing knowledge card in the selected project. When multiple ContextManager projects exist, pass `project` explicitly using the exact project ID, exact project name, or exact workspace root path. Only the fields you supply are changed — omitted fields are left as-is. Use #ctx to find the card ID first.",
				"canBeReferencedInPrompt": true,
				"icon": "$(edit)",
				"inputSchema": {
					"type": "object",
					"required": [
						"id"
					],
					"properties": {
						"project": {
							"type": "string",
							"description": "Exact project ID, exact project name, or exact workspace root path. Required when multiple ContextManager projects exist."
						},
						"id": {
							"type": "string",
							"description": "ID of the knowledge card to edit."
						},
						"title": {
							"type": "string",
							"description": "New title. Omit to keep existing."
						},
						"content": {
							"type": "string",
							"description": "New markdown content. Omit to keep existing."
						},
						"category": {
							"type": "string",
							"description": "New category. Omit to keep existing.",
							"enum": [
								"architecture",
								"pattern",
								"convention",
								"explanation",
								"note",
								"other"
							]
						},
						"tags": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Replacement tag list. Omit to keep existing."
						},
						"source": {
							"type": "string",
							"description": "New source reference. Omit to keep existing."
						}
					}
				}
			},
			{
				"name": "contextManager_organizeKnowledgeCards",
				"tags": [
					"knowledge",
					"card",
					"folder",
					"organize",
					"move",
					"memory"
				],
				"toolReferenceName": "organizeCards",
				"displayName": "Organize Knowledge Cards",
				"modelDescription": "Organizes knowledge cards into folders in the selected project. When multiple ContextManager projects exist, pass `project` explicitly using the exact project ID, exact project name, or exact workspace root path. Actions: 'listFolders' (show all folders and card counts), 'createFolder' (create a folder, optionally nested under parentFolderName), 'moveCard' (move card by ID to a folder by name, auto-creates folder if needed), 'autoOrganize' (auto-assign all unorganized cards to best-matching folders).",
				"canBeReferencedInPrompt": true,
				"icon": "$(folder)",
				"inputSchema": {
					"type": "object",
					"properties": {
						"project": {
							"type": "string",
							"description": "Exact project ID, exact project name, or exact workspace root path. Required when multiple ContextManager projects exist."
						},
						"action": {
							"type": "string",
							"description": "Action to perform. Defaults to 'listFolders' if omitted.",
							"enum": [
								"listFolders",
								"createFolder",
								"moveCard",
								"autoOrganize"
							],
							"default": "listFolders"
						},
						"folderName": {
							"type": "string",
							"description": "Folder name (for createFolder)."
						},
						"parentFolderName": {
							"type": "string",
							"description": "Parent folder name (for creating subfolders)."
						},
						"cardId": {
							"type": "string",
							"description": "Card ID to move (for moveCard)."
						},
						"targetFolderName": {
							"type": "string",
							"description": "Target folder name (for moveCard). Empty = root/uncategorized."
						}
					}
				}
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "npm run check-types && node esbuild.js",
		"check-types": "tsc --noEmit",
		"watch": "npm-run-all -p watch:*",
		"watch:esbuild": "node esbuild.js --watch",
		"watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
		"package": "npm run check-types && node esbuild.js --production",
		"compile:tsc": "tsc -p ./ && node -e \"require('fs').copyFileSync('node_modules/sql.js/dist/sql-wasm.wasm','out/sql-wasm.wasm')\"",
		"lint": "eslint src",
		"test": "vitest run"
	},
	"devDependencies": {
		"@modelcontextprotocol/sdk": "^1.27.1",
		"@types/node": "^22",
		"@types/sql.js": "^1.4.9",
		"@types/vscode": "^1.100.0",
		"@vscode/vsce": "^3.7.1",
		"esbuild": "^0.27.3",
		"npm-run-all2": "^8.0.4",
		"typescript": "^5.9.2",
		"vitest": "^4.0.18",
		"zod": "^4.3.6"
	},
	"dependencies": {
		"@agentclientprotocol/sdk": "^0.16.1",
		"@vscode/prompt-tsx": "^0.3.0-alpha.12",
		"katex": "^0.16.28",
		"sql.js": "^1.14.0"
	}
}
